orbs:
  aws-s3: circleci/aws-s3@1.0.16
version: 2.1
jobs:
  build:  
    docker:
      - image: circleci/node:12
    steps:
      - run: npm install
      - run: npm build
      - aws-s3/sync:
          from: dist
          to: 's3://elem.dashboard/'